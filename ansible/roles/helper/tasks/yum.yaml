---
# YUM package installation tasks

- name: Install YUM packages
  become: true
  ansible.builtin.yum:
    name: "{{ item }}"
    state: present
  loop: "{{ helper_yum_packages }}"
  when: helper_yum_packages | length > 0



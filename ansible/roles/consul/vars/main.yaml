---
# Consul role vars
# Variables in this file take precedence over defaults/main.yaml.
# Only place values here that must NOT be overridden by the operator.
# For tunable defaults, use defaults/main.yaml instead.

---
# Ansible Galaxy requirements file
# Install with: ansible-galaxy install -r requirements.yaml

collections:
  - name: community.crypto
    version: ">=2.0.0"

roles:
  - name: geerlingguy.docker
    version: 7.4.1